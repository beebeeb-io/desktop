{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Beebeeb desktop windows. Targets the settings window plus all on-demand conflict-resolution windows (label `conflict-<file_id>`, opened by the `open_conflict_window` IPC).",
  "windows": ["settings", "conflict-*"],
  "permissions": [
    "core:default",
    "opener:default",
    "window-state:default",
    "updater:default",
    "autostart:default",
    "dialog:allow-open"
  ]
}
